{{#if resume.skills.length}}
<section id="skills">
    <h2>Skills</h2>
    <ul class="skills-list">
        {{#each resume.skills}}
        {{#each keywords}}
        <li>{{{.}}}</li>
        {{/each}}
        {{/each}}
    </ul>
</section>
{{/if}}
